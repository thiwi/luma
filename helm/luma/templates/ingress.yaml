apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: luma-ingress
spec:
  rules:
  - host: luma.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: luma-backend
            port:
              number: 80
